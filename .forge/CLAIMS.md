# Active claims

Who is working on what, right now. One row per branch in flight; the protocol
that says when to add and remove a row is in [`AGENTS.md`](AGENTS.md). This
table has its own file because a claim edit used to land in the file everyone
else was editing too, and three pull requests in one day went dirty on it.

Remove your row when you are done. Stale rows are worse than no rows.

| branch | agent | files / area | started |
| --- | --- | --- | --- |
| codex/pricing-markup-shipping-order | BUG FIXER (Codex 01a079eb) | Urgent owner pricing correction: `src/markup.js` canonical tire formula only; `backend/inventory.mjs` offers `shipping_cents` nullable migration plus offer list/save/catalog projection; `src/owner/OwnerInventory.jsx` global/per-tire shipping controls and canonical suggestion only; focused `backend/owner.test.mjs`, `backend/quotes.test.mjs`, `backend/pricing.test.mjs`, and `backend/mail.test.mjs` coverage. Contract: `(supplier × markup) + shipping`; nullable per-tire override beats global, explicit zero is real; owner price still wins; shipping remains internal and never appears in customer catalog/preview/status/quote/email output. No size-level shipping rule, production write, customer data, request submission, scraper, or unrelated pricing catalogue lines. DECLARED OVERLAP with open PR #431: it changes `backend/inventory.mjs` catalog description normalization and a separate owner-test region; this branch changes the same catalog projection for shipping input only and will preserve/rebase its normalization. | 2026-09-08 |
| codex/status-device-history-recovery | JUNIOR FRONT END DEV 3 (local_16ba9ea6) | `src/routes/Status.jsx` -- `/status` no-device-history empty-state panel only: truthful device-local recovery copy, existing `TEXT_HREF`/`TEXT_LABEL` action, and existing New Request navigation; no request lookup/enumeration, email search, API, auth, or privacy changes. `.forge/responsive-check.mjs` -- one focused no-device-history screen scenario at both configured widths, its phone accessibility measurement, and the two expected-count constants only. Existing direct `?request=<id>` behavior remains untouched and is re-run through the full gate. OVERLAP: `gate-copy-reaches-the-visitor` owns only the landing block in `.forge/dead-end-audit.mjs`; this claim does not touch that file. | 2026-09-07 |
| codex/review-431-normalization | KMT REPO/RELEASE AGENT (project-manager delegated review) | Independent read-only review of PR #431 exact head: supplier-ingress and public catalog plain-text normalization, entities/malformed markup/script-event removal, existing-row read behavior, identity/price invariance, scope/ancestry/merge-tree/CI. No implementation edits, provider requests, production mutation, or merge. | 2026-09-07 |
| codex/catalog-description-normalization | JUNIOR FULL STACK ENGINEER (Codex, project-manager delegated bug fix) | New `backend/catalog-description.mjs` and focused test; `backend/inventory.mjs` only at supplier-row write normalization and customer `catalog()` description projection; public catalog API regression in the existing catalog test region; `package.json`/`package-lock.json` only to add the maintained `parse5` HTML parser required by private review. Convert supplier HTML/entities to plain text at ingress and again at the customer-safe read boundary so existing rows are fixed immediately. No `src/`, scraper/provider behavior, image assets/PR #430 files, social-proof work, production mutation, or HTML rendering. | 2026-09-07 |
| codex/review-430-safety | KMT REPO/RELEASE AGENT (project-manager delegated review) | Independent read-only review of PR #430 exact head: image asset migration/repository, offline mirror safety seams, identity reconciliation, validation/dedupe/provenance/failure state, SSRF/path traversal/payload/storage collision hazards, ancestry/merge-tree/diff/checks. No downloader/provider probes, real downloads, imports, production/Fly changes, implementation edits, or merge. | 2026-09-07 |
| codex/catalog-image-mirror | CATALOG IMAGE MIRROR ENGINEER (Codex delegated task) | New `backend/image-assets.mjs` image-candidate schema/repository, new `scripts/image-mirror.mjs` offline mirror seam, `backend/image-mirror.test.mjs`, and `docs/image-mirroring.md`; stable supplier identity/SKU/product URL reconciliation and asset metadata only. No edits to existing supplier fetch/scraper behavior, customer UI, production import/storage, provider probes, or Fly deployment. | 2026-09-07 |
| codex/social-proof-discoverability | KMT SOCIAL PROOF DISCOVERABILITY ENGINEER (Codex delegated task) | `src/components/SocialProof.jsx` and `.css` marketing copy/layout/link rendering; `src/routes/CustomerRequest.jsx` social-proof/footer placement only; `src/owner/SocialProof.jsx` and `.css` enabled-state labeling only; new `.forge/social-proof-visibility-audit.mjs` browser regression. Keep verified profile URLs evidence-only, no ratings/reviews/follower counts, no request-flow behavior changes, and no backend/metadata changes. | 2026-09-07 |

`scraper-catalog-updater`, `codex/refine-order-flow`, `wire-scraped-catalog` and
`owner-inventory-backend` were all merged into `main` on 2026-09-05 and their
rows removed. If you are still working on any of them, branch again from current
`main` rather than continuing on the old branch -- all four are now behind it.
