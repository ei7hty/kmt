# Active claims

Who is working on what, right now. One row per branch in flight; the protocol
that says when to add and remove a row is in [`AGENTS.md`](AGENTS.md). This
table has its own file because a claim edit used to land in the file everyone
else was editing too, and three pull requests in one day went dirty on it.

Remove your row when you are done. Stale rows are worse than no rows.

| branch | agent | files / area | started |
| --- | --- | --- | --- |
| codex/catalog-product-enrichment | CATALOG DATA / SCRAPER ENGINEER (Codex delegated task) | `scripts/scrape-tires.mjs`, `scripts/giga-tires.mjs`, `scripts/browser-fetch.mjs` (generic product-page fetch method only), `scripts/import-tires.mjs`, scraper fixtures/tests, `src/data/scraped-tires.json` data contract as needed, and `backend/inventory.mjs` plus focused inventory/import tests: bounded product-page enrichment by URL, size, or small batch; durable optional source metadata; migration/import round trips. No customer API/UI and no live crawl or production import. | 2026-09-07 |
| codex/social-integration-spec | SOCIAL / SEO INTEGRATION STRATEGIST (Codex delegated task) | NEW FILE `.forge/social-integration.md` only — docs-only audit/spec for verified owner social URLs, configurable platform/order model, marketing-only placement, JSON-LD `sameAs`, OG assets, analytics/privacy, empty state, staged rollout, and acceptance criteria. No customer-facing links or claims. | 2026-09-07 |
| gate-copy-reaches-the-visitor | GATE ENGINEER (local_4ba48b4c) | `.forge/dead-end-audit.mjs` -- the landing-surface block only (the five presence checks added in #398), plus `EXPECTED_CHECKS`. Ruled by the PROJECT MANAGER; the recipe is SITE COPY ENGINEER's, who routed around my three now-released claims on this file rather than collide on that constant. WHY: nothing asserts that what Ken saves is what a visitor reads. #397 shipped with the registry, store, routes, injection, owner screen and every test individually correct and no component reading any of it -- the screen said "your words are live" while the hero said "Mobile Tire". SITE COPY ENGINEER's own test proves the wiring EXISTS (every registry key referenced by a component); this proves it WORKS at runtime. Different claims, and they said so rather than letting one stand for the other. THE REQUIREMENT, from the PM and drawn from the step I did by hand: the check must verify its own precondition and fail DIFFERENTLY for each broken link -- the API refused the write / the write applied but the server did not inject it / it was injected but no component rendered it. That last is the #397 defect exactly. A check that cannot tell them apart reports 'working' when the write silently failed, which is the whole class this repo has collected five instances of tonight. Shape: PUT an override with a marker through the owner session the audit already holds, confirm the served HTML carries it, reload and assert the marked testid renders it, then POST undo and confirm the shipped wording returns so the run leaves nothing behind. NOT `src/` -- and specifically NOT a testid on the service-strip description: `.service-strip span` is a grid with a 3px gap and wrapping that text node nests a grid in a grid (SITE COPY ENGINEER, measured). NOT `backend/`, NOT the workflow. OVERLAP: none. My only other row is `gate-a11y-failure-mode` (#419), which is `a11y-85-measure.mjs`. | 2026-09-07 |
| owner-password-optional | OWNER AUTH CUTOVER ENGINEER (Codex delegated task) -- transferred from OWNER AUTH ENGINEER (local_907f8d1f) after the user's successful production Google login | ONE BRANCH now: `backend/auth.mjs` (`readAuthConfig`'s boot guard and the login route's empty-password refusal) and NEW FILE `backend/password-optional.test.mjs`. #395 MERGED and live, and the user has now completed the held production Google-login precondition. Preserve commits `2dc5759` and `6e27cfb`; rebase and validate rather than reimplement. This is sequenced after `owner-decision-actor`; do not unset Fly secrets. NOT `backend/quotes.mjs`. | 2026-09-07 |

| owner-decision-actor | OWNER AUTH CUTOVER ENGINEER (Codex delegated task) -- transferred from OWNER AUTH ENGINEER (local_907f8d1f) on the user's explicit instruction | The #290 actor chain, ONE PR because the deliverable is an end-to-end assertion no subset can run. FOUR PARTS: (1) `backend/auth.mjs` -- the Google callback passes the verified email to `sessions.create`, and `createAuth` gains `actorFor(request)`. (2) `backend/quotes.mjs` -- `decide`/`finish`/`cancel` take an `actor`, and `moveTo` throws on an owner transition without one, preserving minted/password markers. (3) `backend/api.mjs` -- resolve `actorFor(request)` once at the top of the quote-action branch, thread it into all three verbs, and refuse a null actor with 401. (4) tests update call sites and directly cover handler refusal for a constructed null-actor session. Do not touch public customer auth or weaken the owner session gate. | 2026-09-07 |
| codex/customer-pricing-preview | CUSTOMER PRICING ENGINEER (Codex delegated task) | Customer pre-submit, server-authoritative charge preview: `backend/api.mjs` public preview path/dispatch only (NOT owner quote-action branch claimed by `owner-decision-actor`); `backend/quotes.mjs` draft-calculation/public-safe preview seam only (NOT decision actor transitions); relevant backend tests; `src/api.js`; `src/routes/CustomerRequest.jsx` review/ZIP-change regions; customer request styles/tests/audits as narrowly required. Show tire, automatic catalogue service, optional disposal/selected lines, subtotal, applicable tax, total, and active ZIP beside mobile/service fee. Never expose supplier shipping/markup; no charge at preview; preserve final owner review. | 2026-09-07 |
`scraper-catalog-updater`, `codex/refine-order-flow`, `wire-scraped-catalog` and
`owner-inventory-backend` were all merged into `main` on 2026-09-05 and their
rows removed. If you are still working on any of them, branch again from current
`main` rather than continuing on the old branch -- all four are now behind it.
