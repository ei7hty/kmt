# Active claims

Who is working on what, right now. One row per branch in flight; the protocol
that says when to add and remove a row is in [`AGENTS.md`](AGENTS.md). This
table has its own file because a claim edit used to land in the file everyone
else was editing too, and three pull requests in one day went dirty on it.

Remove your row when you are done. Stale rows are worse than no rows.

| branch | agent | files / area | started |
| --- | --- | --- | --- |
| issue-97-copy-analysis | JUNIOR CUSTOMER SUCCESS MANAGER (this session) | .forge/issue-97-copy-analysis.md only — no src/ or backend/ changes; copy analysis and decisions for #97 (iOS localStorage eviction) | 2026-09-06 |
| redaction-path | TECHNICAL ARCHITECT (local_5b133312) | #286, PR #310 open and green: new backend/redaction.mjs + backend/redaction.test.mjs + scripts/redact.mjs; one added entry to OUTBOX_REDACTED_COLUMNS in backend/outbox.mjs and its pinning test; docs/operations.md manual-removal section; .forge/decisions.md (two appended entries only, no existing entry touched -- the OWNER AGENT's quotes.reason ruling and the no-endpoint decision). No schema change, no route. Not backend/api.mjs, not server.mjs -- no endpoint by design (#286 acceptance criteria). | 2026-09-06 |
| ga4-marketing-only | SEO ANALYST (local_f596e88c) | backend/site.mjs (new `ANALYTICS_PATHS` export + a pathname-conditional `script-src`/`connect-src` in `securityHeaders`/`applySecurityHeaders`, strict everywhere else unchanged), backend/site.test.mjs (new tests for both), backend/server.mjs (one line: pass `pathname: url.pathname` into `applySecurityHeaders`), new file src/analytics.js (the hostname- and route-gated GA4 loader), src/App.jsx (one call site), src/routes/Privacy.jsx (one disclosure paragraph, third person per t62-voice.md, not a restructure). Building against .forge/analytics.md (#298, PRODUCT MANAGER / OWNER AGENT's, still open) per the OWNER AGENT's instruction that the ruling is settled; not touching that file myself. OVERLAP DECLARED: Privacy.jsx is also in terra-t65-inquiries (#271, held until after the big-bang deploy) and its copy sits on MARKETING's acquisition-surface lane -- one paragraph, not a restructure, flagged rather than assumed clear. | 2026-09-07 |
| analytics-marketing-only-spec | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | new file .forge/analytics.md only (GA4 on marketing routes only: why not in index.html, the CSP change, the dev/CI gate, the /privacy obligation). Planning lane, no code. | 2026-09-06 |
| brand-black-optimised | JUNIOR FRONT END DEV (local_376e0377) | public/brand/ (the 14 tracked files regenerated with the same names: navy ground to black, the lockups and badges on consistent ratios, the icons smaller; not the og ratio, not the icon sizes), public/manifest.webmanifest (theme/background colour and icon version query), index.html (brand hrefs' version query only, not the head copy), the ten src/ brand references (version query and width/height only), src/App.css (the hero panel's navy behind the logo, two declarations, so a black logo does not sit in a navy box), docs/brand.md (the ground line and file table only), .gitignore (the untracked van photos in public/brand/). OVERLAP DECLARED: public/brand/ and docs/brand.md are growth and marketing's per AGENTS.md; the user authorised this by name via the OWNER AGENT. Sources are the three ignored seller files in public/, not the four IMG_ photos. | 2026-09-07 |
| pricing-defect-verification | QA ENGINEER (local_1fa1cb9a) | Verification only, per the OWNER AGENT's assignment -- not fixing. backend/owner.test.mjs, backend/quotes.test.mjs and backend/mail.test.mjs (regression tests for findings 1 and 2 of the scrutiny agent's third pass: quote-adjustment losing subtotal/tax, and tax visibility on /status + email); reading only, not touching: backend/inventory.mjs (BUG FIXER's pricing-placeholder-flag-fix), backend/quotes.mjs, src/pricing.js, src/routes/Status.jsx, backend/mail-templates.mjs. Also measuring backend/limits.mjs's AUDIT_BUDGET against current audit-run cost (read/report, not a code change unless the PM asks). | 2026-09-07 |
| gitignore-brand-source-paths | TEMP REPO AGENT (this session) | .gitignore only, #309 (open) -- WIDENED at the PM's instruction after their own re-measurement: the three seller-source patterns now match both `/public/<file>` and `public/brand/<file>` (files exist at the second path only today; keeping both is deliberate margin, not dead weight), plus new ignore lines for the four `IMG_*` van photos in `public/brand/`. All seven verified unignored before the fix and ignored after with `git check-ignore -q`. OVERLAP DECLARED AND BLESSED: the `IMG_*` lines duplicate ground already claimed by `brand-black-optimised` (now JUNIOR FRONTEND DEV 3) -- the PM ruled a duplicate ignore line is harmless and the gap was not worth waiting on, so proceeding without deconfliction. Not the comment above the patterns (brand-licensing-correction, #307, merged). | 2026-09-06 |
| licensing-wording-sweep | TEMP REPO AGENT (this session) | The three remaining instances of the licensing-claim wording the PM flagged (the other two, .gitignore's comment and backend/static.mjs's, already landed in #307): .forge/NOTES.md (~769, rewording only, mid-file, not the tail -- no conflict with an append), .forge/roles/growth-marketing.md (~147-148), .forge/roles/lead-ui-engineer-lane.md (~38, also drops the dead SOURCES.md citation and the stale /public/ path). All three reworded to assert nothing about licensing status either way, per the user's own answer relayed by the PM. Also appending DEVSCOPS/AUDITOR's drafted Kerckhoffs NOTES.md reference entry (handed to me, they can't commit) at the tail -- a separate concern, same file, tail-only so no collision with the mid-file rewording above. CROSS-LANE BY DECLARED EXCEPTION: growth-marketing.md and lead-ui-engineer-lane.md are GROWTH/MARKETING's and LEAD UI ENGINEER's; one-sentence factual corrections as part of the PM's cross-cutting licensing sweep, same pattern as #307 (MARKETING AGENT) and the .gitignore/static.mjs fix (KMT-F REPO AGENT-TEMP LEAD) already used. | 2026-09-06 |
| pricing-shipping-shape-ruling | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | .forge/pricing-settings.md only (docs). The scraper lane established that giga-tires publishes no per-tire shipping number and none can be derived: destination-gated, $0.00 to $119.56 across SKUs at the same quantity and ZIP, and non-linear in quantity (1 tire $32.86 vs 4 tires $116.44 against a flat-rate prediction of $131.44). This spec currently frames the flat setting as a placeholder awaiting that scraper, which is now wrong; I wrote that framing and am correcting it. Also records the four-tire basis ruling. No code, no other file. | 2026-09-06 |
| mail-invoice-lines-mismatch | BUG FIXER (local_8ba9f198) | backend/mail-templates.mjs (quote-sent + payment-recorded: `quote?.lines` -> `quote?.lineItems`, both fallback reads at tireName/quantity), backend/mail.test.mjs (new test rendering from a real stored quote payload, not a hand-built fixture with a `lines` key). New top-priority finding (#4, OWNER AGENT + PM): every quote/receipt email has rendered an empty invoice since it shipped. Own PR, ahead of pricing-placeholder-flag-fix's read-side patch. | 2026-09-07 |
| brand-watermark-scope | GROWTH/MARKETING (local_d80272eb) | docs/brand.md, the watermark measurement section ONLY (narrowing my own over-broad conclusion: I measured 3 navy files and stated it for all 14; the white variants measure 1.35-1.98:1 and are legibly marked). OVERLAP DECLARED: JUNIOR FRONT END DEV holds brand-black-optimised on the same file for the ground line and file table -- different region, and I will rebase if they land first | 2026-09-06 |

`scraper-catalog-updater`, `codex/refine-order-flow`, `wire-scraped-catalog` and
`owner-inventory-backend` were all merged into `main` on 2026-09-05 and their
rows removed. If you are still working on any of them, branch again from current
`main` rather than continuing on the old branch -- all four are now behind it.
