# Active claims

Who is working on what, right now. One row per branch in flight; the protocol
that says when to add and remove a row is in [`AGENTS.md`](AGENTS.md). This
table has its own file because a claim edit used to land in the file everyone
else was editing too, and three pull requests in one day went dirty on it.

Remove your row when you are done. Stale rows are worse than no rows.

| branch | agent | files / area | started |
| --- | --- | --- | --- |
| issue-97-copy-analysis | JUNIOR CUSTOMER SUCCESS MANAGER (this session) | .forge/issue-97-copy-analysis.md only — no src/ or backend/ changes; copy analysis and decisions for #97 (iOS localStorage eviction) | 2026-09-06 |
| notes-safeguard-deletion-test | TECHNICAL ARCHITECT (local_5b133312) | .forge/NOTES.md, one appended entry at the tail only, at the PROJECT MANAGER's request: deleting the safeguard to check the test notices, after my own rollback test in #310 passed with the transaction removed. OVERLAP DECLARED, three-way: licensing-wording-sweep (TEMP REPO AGENT, carrying DEVSCOPS/AUDITOR's Kerckhoffs entry), local_44d1e1f9's entry, and local_d80272eb's second entry on PR #308 all append to this same tail. Append-only, so these collide at the last line and nowhere else — the resolution the OWNER AGENT already did once tonight on #253. Nothing else in the file; not the mid-file licensing rewording. | 2026-09-06 |
| redaction-path | TECHNICAL ARCHITECT (local_5b133312) | #286, PR #310 open and green: new backend/redaction.mjs + backend/redaction.test.mjs + scripts/redact.mjs; one added entry to OUTBOX_REDACTED_COLUMNS in backend/outbox.mjs and its pinning test; docs/operations.md manual-removal section; .forge/decisions.md (two appended entries only, no existing entry touched -- the OWNER AGENT's quotes.reason ruling and the no-endpoint decision). No schema change, no route. Not backend/api.mjs, not server.mjs -- no endpoint by design (#286 acceptance criteria). | 2026-09-06 |
| ga4-marketing-only | SEO ANALYST (local_f596e88c) | backend/site.mjs (new `ANALYTICS_PATHS` export + a pathname-conditional `script-src`/`connect-src` in `securityHeaders`/`applySecurityHeaders`, strict everywhere else unchanged), backend/site.test.mjs (new tests for both), backend/server.mjs (one line: pass `pathname: url.pathname` into `applySecurityHeaders`), new file src/analytics.js (the hostname- and route-gated GA4 loader), src/App.jsx (one call site), src/routes/Privacy.jsx (one disclosure paragraph, third person per t62-voice.md, not a restructure). Building against .forge/analytics.md (#298, PRODUCT MANAGER / OWNER AGENT's, still open) per the OWNER AGENT's instruction that the ruling is settled; not touching that file myself. OVERLAP DECLARED: Privacy.jsx is also in terra-t65-inquiries (#271, held until after the big-bang deploy) and its copy sits on MARKETING's acquisition-surface lane -- one paragraph, not a restructure, flagged rather than assumed clear. | 2026-09-07 |
| analytics-marketing-only-spec | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | new file .forge/analytics.md only (GA4 on marketing routes only: why not in index.html, the CSP change, the dev/CI gate, the /privacy obligation). Planning lane, no code. | 2026-09-06 |
| brand-black-optimised | JUNIOR FRONT END DEV 3 (local_16ba9ea6) | public/brand/ (the 14 tracked files regenerated with the same names: navy ground to black, the lockups and badges on consistent ratios, the icons smaller; not the og ratio, not the icon sizes), public/manifest.webmanifest (theme/background colour and icon version query), index.html (brand hrefs' version query only, not the head copy), the ten src/ brand references (version query and width/height only), src/App.css (the hero panel's navy behind the logo, two declarations, so a black logo does not sit in a navy box), docs/brand.md (the ground line and file table only). NOT .gitignore -- TEMP REPO AGENT holds that in #309. TRANSFERRED from JUNIOR FRONT END DEV (local_376e0377) via the OWNER AGENT, file list kept. OVERLAP DECLARED: public/brand/ and docs/brand.md are growth and marketing's per AGENTS.md; the user authorised this by name via the OWNER AGENT. src/owner/OwnerInventory.jsx: the icon-64 width/height/version-query attributes ONLY -- LEAD FRONT END DEV holds the tap-target fix (#252) elsewhere in that file. Facts carried in from the handover so they survive it: the ground is #080808 (the site black, already in the theme-color meta and the customer shell), not a new black; the second navy is #0d1c23 in src/App.css's t61 block, not #0d1b24, and the band follows the ground. Sources are the three seller files, which are in public/brand/ and NOT in public/ (measured: public/ root holds no source art; #309 is what ignores them). Measured before starting: the light variants carry the seller mark visibly (kmt-light-800 1.99:1, kens-light-1200 1.38:1, kens-light-600 1.35:1 over the outer 7% band, against docs/brand.md's own 1.61:1 "plainly visible" calibration); the OWNER AGENT ruled remove what flattening removes, do not chase the rest, and record what remains in the PR body. | 2026-09-07 |
| gitignore-wording-followup | TEMP REPO AGENT (this session) | .gitignore only, the IMG_* block's comment (one line: "not sourced from the seller files above" -> drops "seller", same reasoning as licensing-wording-sweep). #309 (the patterns themselves) already merged; this is a same-day follow-up KMT-F REPO AGENT-TEMP LEAD asked for rather than held, since #309's exposure fix was more urgent than this word. | 2026-09-06 |
| licensing-wording-sweep | TEMP REPO AGENT (this session) | The three remaining instances of the licensing-claim wording the PM flagged (the other two, .gitignore's comment and backend/static.mjs's, already landed in #307): .forge/NOTES.md (~769, rewording only, mid-file, not the tail -- no conflict with an append), .forge/roles/growth-marketing.md (~147-148), .forge/roles/lead-ui-engineer-lane.md (~38, also drops the dead SOURCES.md citation and the stale /public/ path). All three reworded to assert nothing about licensing status either way, per the user's own answer relayed by the PM. Also appending DEVSCOPS/AUDITOR's drafted Kerckhoffs NOTES.md reference entry (handed to me, they can't commit) at the tail -- a separate concern, same file, tail-only so no collision with the mid-file rewording above. CROSS-LANE BY DECLARED EXCEPTION: growth-marketing.md and lead-ui-engineer-lane.md are GROWTH/MARKETING's and LEAD UI ENGINEER's; one-sentence factual corrections as part of the PM's cross-cutting licensing sweep, same pattern as #307 (MARKETING AGENT) and the .gitignore/static.mjs fix (KMT-F REPO AGENT-TEMP LEAD) already used. | 2026-09-06 |
| pricing-shipping-shape-ruling | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | .forge/pricing-settings.md only (docs). The scraper lane established that giga-tires publishes no per-tire shipping number and none can be derived: destination-gated, $0.00 to $119.56 across SKUs at the same quantity and ZIP, and non-linear in quantity (1 tire $32.86 vs 4 tires $116.44 against a flat-rate prediction of $131.44). This spec currently frames the flat setting as a placeholder awaiting that scraper, which is now wrong; I wrote that framing and am correcting it. Also records the four-tire basis ruling. No code, no other file. | 2026-09-06 |
| relay-is-not-testimony | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | .forge/NOTES.md, one appended entry only, at the PROJECT MANAGER's request. I asserted the user's motive for making the repo public as fact; they instructed it, they never gave a reason. Names my own instance, #303's form. OVERLAP: NOTES.md is append-only and busy tonight -- conflicts only at the last line, which I have resolved twice already. | 2026-09-06 |
| audit-budget-recount | QA ENGINEER (local_1fa1cb9a) | backend/limits.mjs only (AUDIT_BUDGET.submitsPerEmail 20 -> 22, the re-measured real cost; and the header comment claiming AUDIT_BUDGET is "checked rather than remembered", which is false -- limits.test.mjs checks LIMITS against AUDIT_BUDGET, nothing checks AUDIT_BUDGET against what the gate actually does). At the PROJECT MANAGER's request, following up my own earlier measurement. | 2026-09-07 |
| postcutover-baselines | TEMP REPO AGENT (this session) | New files docs/baselines/2026-09-06-postcutover-kmt-fly-dev.txt and -kensmobiletire-com.txt, plus their two rows in docs/baselines/README.md's table. Captures and Notes sections authored by DEVSCOPS/AUDITOR (read-only, can't commit), handed off to me to place; content unedited, only copied in. Also one NOTES.md entry (the baseline-shelf-life lesson), DEVSCOPS/AUDITOR's text verbatim, appended at the tail per the file's own convention. Not the mid-file licensing rewording or the other tail-appenders' entries. | 2026-09-06 |
| agents-subagent-rule | KMT-F REPO AGENT (local_881ff3b5) | .forge/AGENTS.md ONLY: two additions to the coordination section -- the region rule edits-not-features caution (after "Check before you block"), and a new subagent-owns-its-claim rule (after the claim-straight-to-main paragraph). Assigned by PM/OWNER AGENT; #262 region rule already on main so not rewritten. No lane, route, or other file. | 2026-09-06 |

`scraper-catalog-updater`, `codex/refine-order-flow`, `wire-scraped-catalog` and
`owner-inventory-backend` were all merged into `main` on 2026-09-05 and their
rows removed. If you are still working on any of them, branch again from current
`main` rather than continuing on the old branch -- all four are now behind it.
