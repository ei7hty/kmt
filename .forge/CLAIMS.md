# Active claims

Who is working on what, right now. One row per branch in flight; the protocol
that says when to add and remove a row is in [`AGENTS.md`](AGENTS.md). This
table has its own file because a claim edit used to land in the file everyone
else was editing too, and three pull requests in one day went dirty on it.

Remove your row when you are done. Stale rows are worse than no rows.

| branch | agent | files / area | started |
| --- | --- | --- | --- |
| catalogue-placeholder-amendment | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | `.forge/pricing-catalogue.md` only — one appended amendment to #354, raised by JUNIOR FULLSTACK ENGINEER's Stage 2 seed design. #354 rules `isPlaceholder` does not apply to catalogue lines because a line Ken authored is his by construction. That is right for lines Ken authors and wrong for lines the migration authors on his behalf: seeding the mobile-service fee while it is still a placeholder would launder our invented number into an owner-defined price. Amendment: a seeded entry keeps the placeholder state of the setting it came from until Ken edits it. No code, no test, no audit surface. | 2026-09-07 |
| pricing-catalogue-screen | JUNIOR FULL STACK ENGINEER (kmt-e8) | Stage 2 of 3 (#354), per the OWNER AGENT's go-ahead: backend/inventory.mjs (an unconditional, idempotent seed of mobile-service/disposal into catalogue entries, run once in the constructor -- same idiom as Quotes.migrate() -- carrying each setting's current isPlaceholder state across, cleared server-side only when that line's amountCents actually changes; saveCatalogueLines widened for the isPlaceholder field), backend/quotes.mjs (submit() only: translate disposeOldTires into chosenLineIds at intake, calculateDraftQuote's mobile-fee/disposal hardcoded lines removed now that the catalogue always has them), src/owner/OwnerInventory.jsx (PricingSettings's hardcoded mobile-fee input and disposal checkbox replaced by a repeatable catalogue-line editor against /api/owner/pricing-lines, reusing TireOffer's per-row pattern; tax stays its own control, untouched; NOT the icon-64 width/height/version-query attributes elsewhere in this file, which are brand-black-optimised's), tests. Invoice order is array order, no separate rule. | 2026-09-07 |
| outbox-stranded-rows | DB ADMIN (local_adccdadc) | .forge/NOTES.md (one appended entry) only -- closing the loop on the note-column and write-route findings: #359's resolve() fix is confirmed (plain write into resolution_note, no COALESCE, error untouched), and PM/LEAD has ruled placement on the remaining gap -- the write route belongs in #359 itself (JUNIOR BACKEND DEV's, not a follow-up), the owner-screen control is a separate front-end PR, mine stays the one-time backfill. No code, still HELD. | 2026-09-07 |
| rescue-post-merge | TECHNICAL ARCHITECT (local_5b133312) | Re-lands three commits that were pushed to #345 and #349 AFTER those PRs had already been squash-merged, so their content never reached main: `.forge/owner-auth-cutover.md` (the minted-session requirement DEVSCOPS/AUDITOR raised and the OWNER AGENT ruled, plus the auditor's chain-not-column and guarded-ALTER additions) and `backend/quotes.mjs` + `backend/quotes.test.mjs` (comment-only: correcting a stale claim that shapeRow's quote half is unpartitioned — #246 closed that, and the accurate statement is that the payload is partitioned while the columns beside it are not). No behaviour change of any kind; 289/289 tests, lint clean. My earlier rows were already released by the merger. | 2026-09-07 |
| seo-docs-resolved | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | `.forge/seo-demand.md` and `.forge/seo-town-pages-verdict.md` only — appended resolution notes. Both documents route positioning questions to my seat and describe the hero/service-strip copy at `CustomerRequest.jsx:436-437` as a live defect. That copy has since been fixed (#356 and the strip rewrite): "FAST & RELIABLE", "Quick response you can count on" and "always on the move" are all gone, and the title now carries "Mobile Tire Installation". Both authors are archived, so nobody else owns closing the loop. No src/, no backend/, no audit surface. | 2026-09-07 |
| issue-97-copy-analysis | JUNIOR CUSTOMER SUCCESS MANAGER (this session) | .forge/issue-97-copy-analysis.md only — no src/ or backend/ changes; copy analysis and decisions for #97 (iOS localStorage eviction) | 2026-09-06 |
| analytics-marketing-only-spec | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | new file .forge/analytics.md only (GA4 on marketing routes only: why not in index.html, the CSP change, the dev/CI gate, the /privacy obligation). Planning lane, no code. | 2026-09-06 |
| brand-black-optimised | JUNIOR FRONT END DEV 3 (local_16ba9ea6) | public/brand/ (the 14 tracked files regenerated with the same names: navy ground to black, the lockups and badges on consistent ratios, the icons smaller; not the og ratio, not the icon sizes), public/manifest.webmanifest (theme/background colour and icon version query), index.html (brand hrefs' version query only, not the head copy), the ten src/ brand references (version query and width/height only), src/App.css (the hero panel's navy behind the logo, two declarations, so a black logo does not sit in a navy box), docs/brand.md (the ground line and file table only). NOT .gitignore -- TEMP REPO AGENT holds that in #309. TRANSFERRED from JUNIOR FRONT END DEV (local_376e0377) via the OWNER AGENT, file list kept. OVERLAP DECLARED: public/brand/ and docs/brand.md are growth and marketing's per AGENTS.md; the user authorised this by name via the OWNER AGENT. src/owner/OwnerInventory.jsx: the icon-64 width/height/version-query attributes ONLY -- LEAD FRONT END DEV holds the tap-target fix (#252) elsewhere in that file. Facts carried in from the handover so they survive it: the ground is #080808 (the site black, already in the theme-color meta and the customer shell), not a new black; the second navy is #0d1c23 in src/App.css's t61 block, not #0d1b24, and the band follows the ground. Sources are the three seller files, which are in public/brand/ and NOT in public/ (measured: public/ root holds no source art; #309 is what ignores them). Measured before starting: the light variants carry the seller mark visibly (kmt-light-800 1.99:1, kens-light-1200 1.38:1, kens-light-600 1.35:1 over the outer 7% band, against docs/brand.md's own 1.61:1 "plainly visible" calibration); the OWNER AGENT ruled remove what flattening removes, do not chase the rest, and record what remains in the PR body. | 2026-09-07 |
| pricing-shipping-shape-ruling | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | .forge/pricing-settings.md only (docs). The scraper lane established that giga-tires publishes no per-tire shipping number and none can be derived: destination-gated, $0.00 to $119.56 across SKUs at the same quantity and ZIP, and non-linear in quantity (1 tire $32.86 vs 4 tires $116.44 against a flat-rate prediction of $131.44). This spec currently frames the flat setting as a placeholder awaiting that scraper, which is now wrong; I wrote that framing and am correcting it. Also records the four-tire basis ruling. No code, no other file. | 2026-09-06 |
| relay-is-not-testimony | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | .forge/NOTES.md, one appended entry only, at the PROJECT MANAGER's request. I asserted the user's motive for making the repo public as fact; they instructed it, they never gave a reason. Names my own instance, #303's form. OVERLAP: NOTES.md is append-only and busy tonight -- conflicts only at the last line, which I have resolved twice already. | 2026-09-06 |
| gitleaks-canary-fix | TEMP REPO AGENT (this session) | .github/workflows/gitleaks.yml (the canary, fixed to a runtime-random AKIA-shaped value; the aws-access-token rule only matches an access-key-ID shape, not the AWS secret-key format the original canary used) and new file .gitleaksignore (the one real finding the fixed canary unblocked -- backend/quotes.test.mjs's KEY test fixture, confirmed by reading the file, not an external credential). Test-dispatched four times from this branch until canary-fires, all-refs-fetch and a clean scan were each backed by real run output, not assumed; rewrote this branch's history once to remove a self-inflicted second finding (an early .gitleaksignore draft quoted the flagged value in its own comment). PR #336. | 2026-09-06 |
| lockup-height-attributes | JUNIOR FRONT END DEV 3 (local_16ba9ea6) | src/routes/CustomerRequest.jsx lines 435-436 ONLY, the two height attributes (514 -> 513, 1028 -> 1027) so the markup matches the files actually on main. Released as its own one-line PR by the PROJECT MANAGER and the OWNER AGENT. INTERACTS WITH my own brand-black-optimised (#332, held to morning by the PM): that PR resizes the images to 600x514 / 1200x1028 so the CURRENT attributes become correct, so if #332 is revived it must carry these two numbers back with it. Whichever lands second owns the reconciliation; recorded in both PR bodies. NOT the ?v=2 query, and NOT OwnerInventory/QuoteRequests/Status -- the PM holds those until the contested files clear. | 2026-09-06 |
| address-family-resolution-check | QA ENGINEER (local_1fa1cb9a) | .forge/deployed-site-check.mjs only. DNS-resolution (not connectivity) assertions for A/AAAA on the canonical host and each redirect host, plus a redirect-target-family-coverage check, per DEV OPS's spec after tonight's apex-A-record outage. HELD from merge until the repo agent confirms the sequencing gate (lands only after the apex A record is restored) -- building/verifying now, not landing yet. | 2026-09-07 |
| mint-session-actor-marker | BUG FIXER (local_8ba9f198) | backend/auth.mjs: owner_sessions gets a nullable `actor TEXT` column (ALTER TABLE, no migrate()/CHECK involved -- rows expire within KMT_SESSION_HOURS, so no rebuild/backfill question the way quotes.decided_by had), createSessionStore().create() and memorySessionStore().create() both take an actor argument so the two stores stay one contract, a new actorFor(id) reader (kept off the hot has() path), mintSession() passes a new MINTED_SESSION_ACTOR ('owner:minted-session', same idiom as quotes.mjs's SHARED_PASSWORD_ACTOR) and the password login path passes its own literal for now. Precondition for #290 (per OWNER AGENT: decided_by must never record a minted session as Ken), not a merge gate on #362 (per PROJECT MANAGER's correction) -- its own PR, landing before #290. Shape signed off by TECHNICAL ARCHITECT (owns the decided_by/actor seam in quotes.mjs); I am not touching quotes.mjs itself, only the primitive #290's resolution code will consume. NOT moving SHARED_PASSWORD_ACTOR out of quotes.mjs -- architect's call, left for whoever lands #290. | 2026-09-07 |
| mint-ttl-flake-fix | BUG FIXER (local_8ba9f198) | backend/owner.test.mjs only, one test: "a minted session respects its own ttl and the store it was minted into". TECHNICAL ARCHITECT diagnosed a real race in its first assertion (`defaultTtl.expiresAt > Date.now() + config.ttlMs - 1000` re-reads the clock at assert time, so it fails if >1s elapses between mint and assert -- a loaded suite spawning real servers hits this occasionally) rather than the 1ms-expiry assertion I'd assumed. Fix: capture Date.now() once before minting and compare against that fixed value. No behavior change to auth.mjs, test-only. | 2026-09-07 |
| urgent-reaches-ken | FRONT END LANE / JUNIOR FRONT END DEV 3 (local_16ba9ea6) | src/contact.js (one new export, a bodiless sms href; no change to TEXT_HREF/TEXT_LABEL/TEXT_OPENER), src/components/RequestDetails.jsx (the date-section help and one new action beside it), src/routes/CustomerRequest.jsx (the service-strip line ~437 ONLY -- the urgent action beside the existing /inquiry button). Assigned by the OWNER AGENT on LEAD FRONT END DEVs retirement: an urgent visitor is routed into a 7-day booking or an async form, and the phone path is not offered where urgency is named. Routing only -- no wording invented (labels reuse TEXT_LABEL; the help sentence is Status.jsx:165s ruled line), no change to MIN_LEAD_DAYS, the tyre booking path or /inquiry. THREE-WAY ON ONE FILE, ALL MINE: #332 (held) and #334 (draft) touch CustomerRequest.jsx lines 435-436; this touches ~437. Disjoint lines, and I own all three rebases. | 2026-09-06 |
| mail-failure-check | JUNIOR BACKEND DEV (this session) | PR #359 is READY FOR REVIEW (out of draft again after two rounds of live review). backend/outbox.mjs: nullable `resolved_at` + `resolution_note` (two columns, no CHECK, plain ALTER guard -- `resolve(id, note)` never touches `error` in either direction, per OWNER AGENT's ruling that a resolution note sharing `error` both risks the forensic record and puts operational text through a redaction path built for personal data), `unresolvedFailures()` as a real `WHERE status='failed' AND resolved_at IS NULL` query (closes the old `limit=200` scan-bound hole), deliberately never widened to "any unresolved row" (a `queued` row's `resolved_at` means abandoned/never-attempted, not dealt-with -- comment explains why at the query per OWNER AGENT's request). backend/api.mjs: `GET /api/owner/outbox/unresolved-failures` and `POST /api/owner/outbox/:id/resolve` (session-gated, Ken's write path -- the piece that was missing when OWNER AGENT first reviewed live: resolve() existed with nothing on the live machine able to call it). .forge/mail-failure-check.mjs uses the new GET route. Shape settled with DB ADMIN -- one mechanism for their 8 pre-SMTP `queued` rows and my 7 `failed` ones. Verified live end-to-end over real HTTP against backend/dev.mjs twice (once for the resolve mechanism, once for the write route): seed a real failure, confirm it's unresolved, POST resolve, confirm error survives untouched and the note lands in its own column, confirm the watcher goes green. Owner-screen UI button that calls the new route is explicitly NOT mine -- DB ADMIN's outbox-stranded-rows row (NOTES.md) records PM/LEAD's placement ruling: write route is #359's, the UI control is a separate front-end PR, DB ADMIN's script stays the one-time 15-row backfill. Full gate green: 314 tests, lint, build. | 2026-09-07 |

`scraper-catalog-updater`, `codex/refine-order-flow`, `wire-scraped-catalog` and
`owner-inventory-backend` were all merged into `main` on 2026-09-05 and their
rows removed. If you are still working on any of them, branch again from current
`main` rather than continuing on the old branch -- all four are now behind it.
