# Active claims

Who is working on what, right now. One row per branch in flight; the protocol
that says when to add and remove a row is in [`AGENTS.md`](AGENTS.md). This
table has its own file because a claim edit used to land in the file everyone
else was editing too, and three pull requests in one day went dirty on it.

Remove your row when you are done. Stale rows are worse than no rows.

| branch | agent | files / area | started |
| --- | --- | --- | --- |
| docs-five-and-tires-prefix-falsified | OWNER AGENT (local_44d1e1f9) | `docs/approved-product-images.md` and `docs/image-mirroring.md` ONLY -- prose, no code. #497/#498 (robots.txt guard) and #499 (--validation-count) falsified several load-bearing claims in the operator runbook, and REPO AGENT LEAD routed the docs half to me rather than editing another lane. Corrected: the section arguing the `/tires/` prefix must NOT be relaxed (it was a paraphrase of robots.txt, and relaxing it was right -- 50,000/50,000 sitemap URLs now pass); the section asserting five-per-packet "has not moved" with citations #499 made false; "the owner must supply five real product URLs... this implementation does not discover them" (all four mapping fields derive mechanically from a snapshot row); two "Supply exactly five entries" instances; and a line-12 vs line-326 self-contradiction in image-mirroring.md. ADDED: the three-ceiling table (MAX_VALIDATION_COUNT 100 / candidateLimit 250 / MAX_IMAGE_PACKET_ASSETS 500), because raising one alone does not widen a run. NOT `scripts/` (REPO AGENT LEAD just landed #499 in scrape-tires.mjs), NOT `backend/` or the coordinator/import files (JUNIOR BACKEND DEV is inside them), NOT `scripts/giga-tires.mjs` (DB ADMIN). I cannot merge my own PR -- a second agent reads it. | 2026-09-10 |
| build-image-mapping | DB ADMIN (local_adccdadc) | NEW FILE scripts/build-image-mapping.mjs + its test only. Assigned by the OWNER AGENT: generate the owner's mapping.json (candidates/inputDigest/version) that prepareImagePilot (scripts/image-pilot-packet.mjs) consumes, from the committed src/data/scraped-tires.json snapshot -- pure local computation, no network, structurally incapable of one. Groups by model name, ranks by row count, one tire per model, deterministic tie-breaks stated in the file. Proof is feeding the generated output through the real prepareImagePilot and asserting acceptance, then mutating one field at a time (revision, sku, duplicate id, duplicate url, inputDigest, extra key) and asserting each rejection -- not a hand-written copy of its rules. IMPORTS productUrl from scripts/giga-tires.mjs read-only, does not edit that file. NOT scripts/scrape-tires.mjs (REPO AGENT LEAD), NOT the coordinator/import/publication files (JUNIOR BACKEND DEV, image-pipeline-e2e). No live requests -- both owner authorisations already spent. | 2026-09-10 |
| gate-holes-msys-pathconv | JUNIOR REPO AGENT (local_b2ab10bb) | `.forge/gate-holes.md` only, one appended subsection to §5, routed by GATE ENGINEER (local_4ba48b4c) -- their finding, my writing per their explicit ask. Not the same shape as my three existing §5 entries (code behaves differently on CI than here): this is Git Bash's MSYS layer rewriting `<rev>:<path>` git arguments before git sees them (`origin/main:.forge/x` becomes `origin\main;.forge\x`), so a correct instrument (`git cat-file -e`) answers a question nobody asked, cleanly and confidently wrong -- REPO AGENT LEAD nearly blocked a real, already-merged file as missing on main. Reproduced independently before writing (confirmed the semicolon-mangled fatal error, confirmed `MSYS_NO_PATHCONV=1` fixes it). Releasing my own stale `gate-holes-ci-divergences` row in the same commit -- #472 merged hours ago and I missed releasing it. No code, no other file. | 2026-09-10 |
| `stale-actor-comments` | PROJECT MANAGER (local_675fe058) | `backend/auth.mjs` COMMENT ONLY at 626-634, and `backend/quotes.test.mjs` COMMENT plus one test name at 1772-1780. No behaviour change, no assertion change. Both comments assert the opposite of the code directly beneath them: auth.mjs says the verified address "is not recorded on the session yet" one line above `sessions.create(expiresAt, decision.email)`; quotes.test.mjs says "nothing passes one today" when `api.mjs:562` does and no fallback remains. Found while verifying the actor chain for the OWNER AGENT. NOT `.forge/*`, NOT `.github/`, NOT `src/` -- no overlap with the five active lanes. | 2026-09-09 |
| gate-holes-notes | JUNIOR REPO AGENT (local_b2ab10bb) | NEW FILE `.forge/gate-holes.md` and one pointer line in `.forge/NOTES.md`'s index, if it has one. Assigned by the OWNER AGENT: record four environment defects found tonight (src tests invisible to CI, three image suites failing at module load without a Python env, the shared checkout drifting silently, EXPECTED_CHECKS arithmetic in owner-inventory-audit.mjs) before the session holding them ends. NOT `.forge/*.mjs` -- that's GATE & RELEASE ENGINEER's (local_881ff3b5), who is acting on holes 1 and 2; I document, they fix. Every claim verified against `origin/main` before writing, not copied from the assignment on trust -- one did not hold as given (see PR body). | 2026-09-10 |
| `audit-cookie-scope` | SUBAGENT, held by the session that spawned it (a subagent has no row of its own -- `AGENTS.md`) | `.forge/audit-ui.mjs` ONLY, and inside it REGION `signInIfAsked` only: (a) scope the minted owner session cookie to the origin instead of `page.url()`, whose path scopes the session to one path; (b) move the minted-session branch above the `waitForSelector` that waits for the screen to declare itself, so a minted run does not burn 15s waiting for a form that cannot appear. The selector list in that wait is left byte-identical -- the rewrite that exists elsewhere is coupled to UI not on `main`. REGION OVERLAP, reported not resolved: the `codex/owner-product-photo-activation` row below claims "`.forge/audit-ui.mjs` minted-session cookie path fix only", which is the same file, region and fix. That branch is unpushed (local only, `codex/owner-product-photo-activation` @ 58dedd1, absent from `origin`) and this work is an explicit salvage of exactly those two hunks, so the two are the same change rather than competing ones -- but if OWNER OPERATIONS ENGINEER is live, this row is the collision and they win. NOT `.github/workflows/**`, NOT `src/owner/**` (#455), NOT `backend/**`. | 2026-09-09 |
| `image-approval-eligibility` | OWNER OPERATIONS ENGINEER | Superseding my own `codex/owner-product-photo-activation` row: that branch (unpushed, 58dedd1) predated #452/#453/#454, which shipped the photo-approval screen and owner-asset route it also built -- rebasing would have reverted live work, so it is abandoned rather than pushed. REGION now narrow: `backend/image-publication.mjs` (`approvalIssues`, and `review()`'s new `eligibility: {approve, revoke, issues}` field only, nothing else in the file), `backend/image-publication.test.mjs` (new tests only, appended), `src/owner/Images.jsx` (the `blocked`/`ip-issues` gating on the existing Approve button only, no structural changes), `src/owner/Images.css` (`.ip-issues` only). Fixes the defect OWNER AGENT named: Ken could press Approve on an ineligible packet and learn it only from the refusal; this computes the same check `decide()` already enforces and shows it before the click. NOT `.forge/audit-ui.mjs` -- yielding that fix to `audit-cookie-scope` (row above), which is already live on it; my copy of that fix is dropped, not pushed. NOT `readOwnerAsset`/the ordinal route -- #453 already shipped the correct unbounded version; not touching it. NOT `OwnerInventory.*`, NOT any new photo screen. | 2026-09-10 |
| branch-inventory-table | JUNIOR REPO AGENT (local_b2ab10bb) | NEW FILE `.forge/branch-inventory.md`, and one small appended entry in `.forge/NOTES.md`. Per the OWNER AGENT, following the standing branch-cleanup authorisation: a full table of all 244 remote branches (PR state, base, merge SHA, worktree-held y/n, ancestor-of-main y/n), plus the methodology finding that ancestor-of-main misreports ~87% of merged branches here since this repo squash-merges most PRs -- an anti-signal, not a weak one, since it rejects genuinely spent branches and would have passed the two actually-dangerous ones (both ancestor:yes, both worktree-held). Does not delete anything further; 5 already deleted and reported separately. NOT `.forge/*.mjs`, NOT `.forge/gate-holes.md` (separate row). | 2026-09-10 |
| decoder-worktree-cwd-fix | JUNIOR BACKEND DEV (local_0b9989ef) | `backend/fixtures/image-provider/decoder-fixtures.mjs` (the default-venv lookup, extracted into an exported pure function `defaultDecoderPython(moduleDir, platform)`) and NEW FILE `backend/decoder-fixtures.test.mjs`. Routed by the OWNER AGENT via GATE ENGINEER's measurement (`gate-test-count-baseline` row above): `resolve('decoder.local', ...)` resolved against `process.cwd()`, not this file, so it only found the venv when node ran from the repo root -- every worktree checkout (AGENTS.md's own convention) came back `null`, silently dropping 45 tests across three files (measured: 587/584/3 in a worktree vs 632/632/0 in the main checkout). Fix resolves from `import.meta.url` instead, and additionally climbs past a literal `.worktrees/<name>/` segment to the main checkout, so the one venv `docs/image-mirroring.md` already documents building there is found by every worktree without `KMT_IMAGE_DECODER_PYTHON` set by hand. Verified live in both directions in a real worktree (632/632 with the fix, reproduced the exact 587/584/3 by reverting it, restored) before writing the test file; the test itself is mutation-verified (disabled the `.worktrees` climb, confirmed exactly the test naming that behaviour goes red, restored). Test file placed flat in `backend/`, not nested under `fixtures/image-provider/`, because CI's actual glob (`fly-deploy.yml`: `backend/*.test.mjs`) is non-recursive and a nested test would never run. NOT `.forge/gate-holes.md` or `.forge/test-count-check.mjs` -- GATE ENGINEER's, separate rows. NOT the flaky `image-decoder.test.mjs:75` timing test GATE ENGINEER flagged in passing -- unclaimed, unrelated to this bug, not touching it. PR #464 open, awaiting a reader. | 2026-09-10 |
| worktree-remove-by-path | JUNIOR BACKEND DEV (local_0b9989ef) | `scripts/worktree.mjs` and its tests (NEW FILE `scripts/worktree.test.mjs` if none exists). Routed by the OWNER AGENT: extend `remove` to accept a full path, not just the `<name>` form under `.worktrees/`, so REPO AGENT LEAD's cleanup (223 registered worktrees, 86 provably stale, most living outside `.worktrees/` -- under `~/.codex/worktrees/...` and elsewhere) can actually reach them instead of hand-rolling junction removal. ADDITIVE ONLY: the bare-name form's behaviour is unchanged. GUARDS: refuse a target not registered to this repo's own `.git` (a bare-name-shaped input still resolves under `.worktrees/` exactly as today; anything containing a path separator is resolved as a path and checked against `git worktree list`'s own registry, which is inherently scoped to the repo it is run in); refuse a dirty tree INCLUDING untracked files, checked explicitly rather than left to git alone (confirmed empirically first that a bare `git worktree remove` already refuses on untracked files today, exit 128 -- so this is a clearer, earlier message on top of a working backstop, not the only guard); keep the existing junction-unlink-then-remove-without---force order exactly as is, no force flag added anywhere. FOUR PROOFS DEMANDED BY THE BRIEF, all to be run for real and reported with exit codes: removes a registered stale worktree; refuses an unregistered path; refuses a dirty (including untracked) tree; the shared `node_modules` install survives a removal (verified, not reasoned about -- create a throwaway linked worktree, remove it with the tool, confirm package count/`vite`/`.bin` still present in the main checkout's install). NOT `.github/workflows/**` (REPO AGENT LEAD's), NOT `.forge/*.mjs` (GATE ENGINEER's), NOT any teardown-is-structural-not-remembered mechanism -- that is a separate piece the OWNER AGENT put to REPO AGENT LEAD. FOLLOW-UP (same row, same branch, PR #467): strengthened the dirty-tree guard's own code comment per review -- it is load-bearing on its own (runs before `unlink`), not merely a clearer message in front of git's later refusal. Then, gated on GATE ENGINEER's own reproduction (not a guess): `add` had the identical danger `remove` did -- `git worktree add <tree> -b <branch> <ref>` creates the branch and attaches the worktree as two non-atomic effects, and a failure between them (reproduced directly: pre-create a branch, then `add` the same name) threw an uncaught exception (raw Node.js stack, no message, no clean exit code) instead of refusing loudly, leaving an orphaned branch that poisoned a retry a second, more confusing way. Fixed with a pre-flight `branchExists` check (refuses cleanly, names the actual situation) plus a try/catch around the `git worktree add` call itself (prints the real git error, deletes any branch the failed attempt created, exits 1). Both guards mutation-tested: disabling the pre-flight check lets git's own more confusing error through; disabling the try/catch reproduces the exact original raw stack trace. Two new tests in `backend/worktree.test.mjs` (11/11 passing). Checked whether an invalid `--from` ref exercises the branch-cleanup path specifically: it does not -- git validates the ref before creating any branch in that case -- so that one line is verified by inspection rather than a forced repro (forcing "branch created, attach fails" deterministically would mean tampering with permissions on the shared `.worktrees/` directory while other sessions may be creating trees there). Committed, rebased onto origin/main, force-pushed (own branch, own rebase) to PR #467; full gate green (11/11 worktree tests, eslint clean, build clean; the 3 unrelated `backend/*.test.mjs` failures are the pre-existing `decoder.local`/cwd issue from #464, this branch predates that fix). PR #467 needs a fresh read -- substantial addition since JUNIOR REPO AGENT's last look. Released: oauth-callback-path-tie (#465 merged). Still open: decoder-worktree-cwd-fix (#464, awaiting a reader). | 2026-09-10 |

| `worktree-claim-wrapper` | JUNIOR BACKEND DEV (local_0b9989ef) | NEW FILE `scripts/claim.mjs` and its tests (NEW FILE, flat in `backend/` for now -- `scripts/` has zero CI coverage today, same reason `backend/worktree.test.mjs` and `backend/decoder-fixtures.test.mjs` both live there instead of beside the modules they test; will move if/when REPO AGENT LEAD's glob widening lands). Routed by the OWNER AGENT via REPO AGENT LEAD, replacing the 7-line manual recipe at `AGENTS.md:169-176` (plain worktree, edit, commit, fetch, rebase, push HEAD:main, remove) with one call, generalized to any direct-to-main edit rather than hardcoded to `CLAIMS.md`'s format. TWO HARD CONSTRAINTS from the OWNER AGENT, relayed by REPO AGENT LEAD: (a) fail-safe not fail-open -- if the disposable worktree cannot be created, refuse loudly and exit non-zero; NEVER fall back to running the edit against the shared main checkout (this is not hypothetical -- GATE ENGINEER hit exactly this an hour ago, worktree tooling failed and the fallback was the shared checkout, which at that moment held four of my own uncommitted files); (b) no `--force` anywhere, and the disposable tree is always removed in a `finally` regardless of outcome, but a failed push must be surfaced distinctly afterward, never masked by a clean-looking teardown -- a silent lost claim is worse than a loud failed one. Plain `git worktree add` (no junction; derives `ROOT` independently rather than importing `worktree.mjs`'s, since that export is new on PR #467 and not yet on every branch this can run from -- never touches `worktree.mjs`'s `add()`/`link()` either way). NOT `AGENTS.md:169-176` itself -- REPO AGENT LEAD is replacing that recipe now that the verb + signature are settled (pinged). NOT the full-path extension to `scripts/worktree.mjs remove` -- already done, PR #467. DONE: found and fixed one real bug via mutation testing before it shipped (an early `return` inside a try-with-finally only unwinds to the `finally`, not past the rest of its own enclosing function, so every early failure was silently exiting 0 -- fixed by extracting the risky steps into a function that returns an outcome instead of exiting directly) plus an unwrapped `git fetch` that would have crashed raw on a broken origin exactly like the failure this tool exists to prevent. Tested against a fully throwaway sandbox repo, never the real origin (`backend/claim.test.mjs`, 7 tests, including the fail-safe guard for real and a permanently-rejecting push exhausting all retries); all three safety-critical guards mutation-tested by hand. Full gate green (7/7 new tests, 667/667 wider suite, eslint clean, build clean). PR #474 open, awaiting a reader. | 2026-09-10 |

| `scripts-git-lib` | JUNIOR BACKEND DEV (local_0b9989ef) | NEW FILE `scripts/lib/git.mjs` and its tests. Approved by the OWNER AGENT after I surveyed `scripts/` for the defect class named tonight (a failure path that crashes raw or reports success): found 4 live instances across `scripts/worktree.mjs` and `scripts/claim.mjs` in one night, one author -- not carelessness, a missing abstraction. Two functions: `git(args, cwd)` never throws, prints the real git error unconditionally before the caller can decline to, returns `{ ok: true, stdout }` or `{ ok: false, error }` with NO `stdout` key on failure (per OWNER AGENT's correction -- an empty-string `stdout` on failure is a silent wrong answer, the exact class this exists to end, so a caller that forgets to check `.ok` gets `undefined` and throws on the next use instead of quietly proceeding). `gitProbe(args, cwd)` is the separate verb for exit-code-as-answer commands (`merge-base --is-ancestor`, `rev-parse --verify` on a possibly-absent ref) -- prints nothing, since "no" there is a legitimate answer, not a failure; a separate function rather than an option, since an option is a thing to remember and remembering is what this removes. THIS PR: the lib + its own tests only, standalone, so it can merge before either consumer -- follow-up commits then land on PR #467 (worktree.mjs's 5 remaining unwrapped sites, including the sharpest case: `branch -D` inside an existing catch block, where a failure throws a second raw error while already handling the first) and PR #474 (claim.mjs's simplification) once this is on main. NOT touching any call site outside `scripts/` -- OWNER AGENT routes those once I report the count (0, this task is scripts/-only by design). Mutation-tested per OWNER AGENT's explicit requirement: a failing call prints the error and returns ok:false; a caller that ignores `.ok` and reads `.stdout` throws rather than proceeding on `undefined` -- that second one is the test that pins the corrected property. MERGED (PR #475, 01efd2f). FOLLOW-UP, new branch `claim-adopt-git-lib`: `scripts/claim.mjs`'s simplification to use the merged lib instead of its own locally-defined throwing `git()` -- the other consumer named above. `scripts/worktree.mjs`'s 5 remaining sites are landing as a commit on the still-open PR #467 instead, since that branch hasn't merged yet and can absorb it directly. | 2026-09-10 |

| `worktree-adopt-git-lib` | JUNIOR BACKEND DEV (local_0b9989ef) | `scripts/worktree.mjs` only -- adopting `scripts/lib/git.mjs` (PR #475, merged) at its 5 remaining unwrapped call sites, closing out the OWNER AGENT-approved survey work alongside PR #480 (claim.mjs's side, OPEN -- mergeable clean, all checks green, awaiting merge; this row wrongly said "merged" before, caught and corrected by the OWNER AGENT). Sites: `registered()`'s `worktree list --porcelain`; `remove()`'s `status --porcelain`/`rev-parse`/`log --oneline` trio; the `branch -D` cleanup in `add()`'s catch block and in `remove()`'s merged-branch path -- the sharpest of the five, a git call INSIDE an existing catch block, where its own failure used to throw a second raw error while already handling the first. NEW PR, not a follow-up commit on #467 -- that branch already merged (dee1e88) before this started. Mutation-testing the affected guards again since the internal error-propagation shape changes (throw/catch to `.ok` checks) even though external behavior does not. NOT touching `add()`/`remove()`'s own logic beyond the git-call shape -- the `--compare-ref` parameterization and the CREATE-path hardening are already merged and unchanged here. | 2026-09-10 |


| `image-decoder-cpu-timing` | JUNIOR BACKEND DEV (local_0b9989ef) | `backend/image-decoder.test.mjs` ONLY, the `OS CPU limit terminates an isolated noncooperative worker` test's `spawnSync` timeout. Reported by GATE ENGINEER: observed exit 1 on a green count once in two runs -- the assertion (`result.error` must be `undefined`, proving the OS killed the worker before the 8000ms harness timeout did) lost the race by 4ms under nothing worse than Node's own parallel test-file execution. About to become a hard gate once #482 wires the count-check into CI. ROOT CAUSE, measured rather than assumed: `constrain()`'s Windows Job Object limit is `PROCESS_TIME` -- CPU time actually consumed, not wall-clock -- and measured standalone (own machine, real ambient load from other concurrent sessions, no additional noise added) at 4.5-7s to kill a worker with a 1000ms CPU budget, up to 14s with ~20 CPU-bound processes competing for 12 cores. The 8000ms ceiling had roughly 1000ms of real margin. FIX: raised the `spawnSync` timeout to 20000ms -- the CPU budget itself (1000ms) is unchanged, only the wall-clock race margin. Mutation-tested the assertion's discriminating power is preserved, not just loosened: removed the `constrain()` call entirely (simulating the OS limit never applying), confirmed the test now correctly fails via `spawnSync`'s own `ETIMEDOUT` at the new 20s ceiling rather than silently passing, restored. NOT touching `.forge/test-count-check.mjs` or `#482`/`#484` -- GATE ENGINEER's and REPO AGENT LEAD's lanes, unrelated to this timing fix. | 2026-09-10 |



`scraper-catalog-updater`, `codex/refine-order-flow`, `wire-scraped-catalog` and
`owner-inventory-backend` were all merged into `main` on 2026-09-05 and their
rows removed. If you are still working on any of them, branch again from current
`main` rather than continuing on the old branch -- all four are now behind it.
