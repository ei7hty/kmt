# Active claims

Who is working on what, right now. One row per branch in flight; the protocol
that says when to add and remove a row is in [`AGENTS.md`](AGENTS.md). This
table has its own file because a claim edit used to land in the file everyone
else was editing too, and three pull requests in one day went dirty on it.

Remove your row when you are done. Stale rows are worse than no rows.

| branch | agent | files / area | started |
| --- | --- | --- | --- |
| decided-by-no-default | TECHNICAL ARCHITECT (local_5b133312) | backend/quotes.mjs (`moveTo`: drop the `?? SHARED_PASSWORD_ACTOR` default so an owner decision records only an actor a caller actually established, plus the constant's now-stale comment) and backend/quotes.test.mjs. Defect found by the OWNER AGENT and verified by me: `decide()` takes no `actor`, so every owner decision records `owner:shared-password` regardless of how the owner authenticated — true today with no OAuth client, false the moment the user configures one, and it contradicts migrate()'s own comment 360 lines above refusing to write that sentinel where it is not known true. Null means "not recorded"; the sentinel means "recorded as the shared credential", and only a caller that can establish the session's identity may write it. NOT backend/auth.mjs or api.mjs — the `actorFor` threading is OWNER AUTH ENGINEER's follow-up, blocked on #374, and this is only the default that would poison the window before it. | 2026-09-07 |
| gate-signin-empty-password | GATE ENGINEER (local_4ba48b4c) | `.forge/audit-ui.mjs` -- `signInIfAsked`'s PASSWORD BRANCH ONLY (the `page.fill('#owner-password')` / `page.click` / `waitForSelector` at the end of the function). NOT the minted-session branch above it, NOT `openOwnerQuotes`, NOT any other export in that file. Plus NEW FILE `.forge/signin-control.mjs`. Row one of two, sequenced and authorised by the PROJECT MANAGER; row two (the derived state list + status helper + `a11y-85-control.mjs`) comes after and will be its own row -- drawn at the start rather than widened, per the OWNER AGENT. The defect, measured: a `dead-end-audit` run that PASSED 83 of 83 produced `login: wrong password from 127.0.0.1` three times in its own server's log. The audit only ever holds the correct password, so the form is submitted before `page.fill` has committed. `backend/limits.mjs` allows 5 free failures per address per 15 min then doubles a delay from 2s, and `AUDIT_BUDGET.loginFailures` declares 0 expected -- so idle runs stay under the limit and go green while burning the budget silently. Scope is larger than the original ruling and the PM has taken the larger version: since #401 the CI audit step mints a session and deliberately does not export `KMT_OWNER_PASSWORD`, so a log assertion pointed at a CI run would assert zero against a path CI no longer walks -- a check that cannot fail. The control boots its own server WITH a password and drives the password branch deliberately. NOT `.github/workflows/` (DEV OPS / JUNIOR REPO AGENT), NOT `backend/limits.mjs` or its test (the tautological `assert.equal(AUDIT_BUDGET.loginFailures, 0)` is a finding I reported, not a file I am editing), NOT `scripts/mint-session.mjs`. OVERLAP: none. My other rows (#402 `dead-end-audit.mjs`, #406 `request-flow-check.mjs`) touch neither file. | 2026-09-07 |
| agents-md-hands-fixes | JUNIOR REPO AGENT (local_b2ab10bb) | `.forge/AGENTS.md` only, two additions the PROJECT MANAGER asked for directly (a routing that had twice been announced to other people and never reached me): (1) the claims recipe's `git -C <path>`, never `cd`, gets its reasoning recorded next to it -- a failed `cd` mid-chain leaves the shell in the wrong directory, and a later `git push origin HEAD:main` written for a throwaway claims worktree can push a whole feature branch to main instead (the #403 mechanism); (2) the git-discipline section's existing `--force` warning gets strengthened to also name a raw `rm -rf` on a linked worktree explicitly, not just the git `--force` flag, since that is the actual path that emptied the shared node_modules install tonight -- pointing at `scripts/worktree.mjs remove` as the one safe command. Not attributing the incident to any session, per the PM's explicit instruction. No other section touched. | 2026-09-07 |
| gate-owner-api-401-premerge | GATE ENGINEER (local_4ba48b4c) | `.forge/request-flow-check.mjs` only -- a new block asserting the owner API is shut, plus its `EXPECTED_CHECKS`. Authorised by the OWNER AGENT. The exposure: the ONLY end-to-end proof that `/api/owner/*` refuses without a session is `deployed-site-check.mjs`, which runs AFTER the merge has already deployed. A change that opens that door merges green and ships before anything notices -- and the password cutover is exactly when that matters, because a password was hard to lose by accident and an OAuth misconfiguration is not. Why the backend suite does not already cover this: `quotes.test.mjs`'s `serve()` is a hand-rolled reconstruction -- its own comment says "A server shaped like server.mjs: the same gate, the same handlers" -- so it can pass while the real `backend/server.mjs` is open. The gate's audit step boots the real one; this asserts against that. Three checks: `/api/owner/inventory` and `/api/owner/requests` answer 401 with no session, AND `/api/catalog` still answers 200 -- the third is a positive control, so a server refusing everything cannot pass the first two by accident. NOT `dead-end-audit.mjs` (I hold two regions there already, #402 open) and NOT `.github/workflows/` (JUNIOR REPO AGENT holds it for #401). NOT `backend/` -- no production code, audit only. | 2026-09-07 |
| gate-pay-click-guard | GATE ENGINEER (local_4ba48b4c) | `.forge/dead-end-audit.mjs`, the `Pay $` click at line 268 ONLY (plus its comment). Second region of that file for me; my other row (gate-marketing-surface-presence, #398) holds the landing-surface block and `EXPECTED_CHECKS`, and the two do not touch the same lines. The amplifier the OWNER AGENT characterised in #392's failed run: lines 254 and 261 read the sent-note and the Pay button with `.catch(() => false)` and record clean failures, but line 268's `page.click('button:has-text("Pay $")')` is unguarded, so a missing button throws, `main()`'s catch fires, and 30 checks never run -- 48 of 78 instead of a legible failure. This guards the click so a state race records a failure instead of destroying the evidence of everything downstream of it. NOT a fix for the underlying state-propagation race (the adjustment not landing before `/status` is read) -- that cause is still open and is not mine; this only stops the cascade. NOT the ~60 remaining text locators FRONT END ENGINEER scoped out of #375. SEQUENCED AFTER #398, which is open on this same file: I own the rebase and will not ask anyone to merge these out of order. | 2026-09-07 |
| codex/issue-84-catalog-budget | CODEX (current session) | `.forge/deployed-site-check.mjs` only -- issue #84 remaining deployed check: assert the compressed `/api/catalog` transfer stays inside the roadside-phone budget. No backend/API/app changes; `Cache-Control: public, max-age=300` is already on `main`. | 2026-09-07 |
| resolve-stranded-outbox-rows | DB ADMIN (local_adccdadc) | PR #389 is OPEN, awaiting a second reader (I don't merge my own). scripts/resolve-outbox-rows.mjs and one closing .forge/NOTES.md entry. The one-time historical backfill for the eight stranded outbox rows (#367's follow-through, now that #359 is merged at dcec54b and resolved_at/resolution_note/resolve() are confirmed live on main). Frozen ids, exact-count assertion, per-row live cross-check against request_id/status before any write, named-mismatch abort (all eight or none). Same trust boundary as scripts/redact.mjs -- read-only dry run by default, --write required, run by a human with flyctl, NOT executed by me or in this PR. Re-verified against the actual merged outbox.mjs (not the pre-merge diff): 318/318 tests, lint clean. Full design history in .forge/NOTES.md's "outbox-stranded-rows" entries. | 2026-09-07 |
| state-status-rename | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | `.forge/state.json` only — executing the PROJECT MANAGER's ruling, not making one. Renames all 77 `status` fields to `statusAtLastReconcile` so the value cannot be read without reading that it is historical, and restates the reconcile timestamp in `summary`. Their file and their call; they ruled it and offered the edit to whoever held the next `.forge/` PR, and ruled the decision does not count until it is on main. Verified safe first: nothing parses this file — its only reference in the repo is `fly-deploy.yml`'s `paths-ignore`, so this is a no-deploy change with no programmatic reader to break. No milestone or task content altered, no status VALUE changed. | 2026-09-07 |
| sprint-next-corrections | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | `.forge/sprint-next.md` only — two in-place corrections to my own sentences plus one appended section, all from OWNER AUTH ENGINEER's (local_907f8d1f) first-hour verification and re-verified by me. (1) "`signInIfAsked` is imported by five audit scripts" is wrong: three import it directly, two reach it via `openOwnerQuotes`; the set of five affected audits is right, the verb is not — and the verb is what caused the original error. (2) "load-bearing across the whole gate" overstates it: CI runs only dead-end-audit, request-flow-check and responsive-check; owner-inventory-audit and a11y-85-measure are by-hand. (3) Appended: the OAuth precondition is largely already built — audit-ui.mjs:34/56-64 carries the full minted-session path — so what remains is CI wiring in .github/workflows/, not an audit conversion. No code, no test, no audit surface. | 2026-09-07 |
| catalogue-placeholder-amendment | PRODUCT MANAGER / OWNER AGENT (local_44d1e1f9) | `.forge/pricing-catalogue.md` only — one appended amendment to #354, raised by JUNIOR FULLSTACK ENGINEER's Stage 2 seed design. #354 rules `isPlaceholder` does not apply to catalogue lines because a line Ken authored is his by construction. That is right for lines Ken authors and wrong for lines the migration authors on his behalf: seeding the mobile-service fee while it is still a placeholder would launder our invented number into an owner-defined price. Amendment: a seeded entry keeps the placeholder state of the setting it came from until Ken edits it. No code, no test, no audit surface. | 2026-09-07 |
| outbox-stranded-rows | DB ADMIN (local_adccdadc) | .forge/NOTES.md (one appended entry) only -- closing the loop on the note-column and write-route findings: #359's resolve() fix is confirmed (plain write into resolution_note, no COALESCE, error untouched), and PM/LEAD has ruled placement on the remaining gap -- the write route belongs in #359 itself (JUNIOR BACKEND DEV's, not a follow-up), the owner-screen control is a separate front-end PR, mine stays the one-time backfill. No code, still HELD. | 2026-09-07 |
| rescue-post-merge | TECHNICAL ARCHITECT (local_5b133312) | Re-lands three commits that were pushed to #345 and #349 AFTER those PRs had already been squash-merged, so their content never reached main: `.forge/owner-auth-cutover.md` (the minted-session requirement DEVSCOPS/AUDITOR raised and the OWNER AGENT ruled, plus the auditor's chain-not-column and guarded-ALTER additions) and `backend/quotes.mjs` + `backend/quotes.test.mjs` (comment-only: correcting a stale claim that shapeRow's quote half is unpartitioned — #246 closed that, and the accurate statement is that the payload is partitioned while the columns beside it are not). No behaviour change of any kind; 289/289 tests, lint clean. My earlier rows were already released by the merger. | 2026-09-07 |
| brand-black-optimised | JUNIOR FRONT END DEV 3 (local_16ba9ea6) | public/brand/ (the 14 tracked files regenerated with the same names: navy ground to black, the lockups and badges on consistent ratios, the icons smaller; not the og ratio, not the icon sizes), public/manifest.webmanifest (theme/background colour and icon version query), index.html (brand hrefs' version query only, not the head copy), the ten src/ brand references (version query and width/height only), src/App.css (the hero panel's navy behind the logo, two declarations, so a black logo does not sit in a navy box), docs/brand.md (the ground line and file table only). NOT .gitignore -- TEMP REPO AGENT holds that in #309. TRANSFERRED from JUNIOR FRONT END DEV (local_376e0377) via the OWNER AGENT, file list kept. OVERLAP DECLARED: public/brand/ and docs/brand.md are growth and marketing's per AGENTS.md; the user authorised this by name via the OWNER AGENT. src/owner/OwnerInventory.jsx: the icon-64 width/height/version-query attributes ONLY -- LEAD FRONT END DEV holds the tap-target fix (#252) elsewhere in that file. Facts carried in from the handover so they survive it: the ground is #080808 (the site black, already in the theme-color meta and the customer shell), not a new black; the second navy is #0d1c23 in src/App.css's t61 block, not #0d1b24, and the band follows the ground. Sources are the three seller files, which are in public/brand/ and NOT in public/ (measured: public/ root holds no source art; #309 is what ignores them). Measured before starting: the light variants carry the seller mark visibly (kmt-light-800 1.99:1, kens-light-1200 1.38:1, kens-light-600 1.35:1 over the outer 7% band, against docs/brand.md's own 1.61:1 "plainly visible" calibration); the OWNER AGENT ruled remove what flattening removes, do not chase the rest, and record what remains in the PR body. | 2026-09-07 |
| gitleaks-canary-fix | TEMP REPO AGENT (this session) | .github/workflows/gitleaks.yml (the canary, fixed to a runtime-random AKIA-shaped value; the aws-access-token rule only matches an access-key-ID shape, not the AWS secret-key format the original canary used) and new file .gitleaksignore (the one real finding the fixed canary unblocked -- backend/quotes.test.mjs's KEY test fixture, confirmed by reading the file, not an external credential). Test-dispatched four times from this branch until canary-fires, all-refs-fetch and a clean scan were each backed by real run output, not assumed; rewrote this branch's history once to remove a self-inflicted second finding (an early .gitleaksignore draft quoted the flagged value in its own comment). PR #336. | 2026-09-06 |
| lockup-height-attributes | JUNIOR FRONT END DEV 3 (local_16ba9ea6) | src/routes/CustomerRequest.jsx lines 435-436 ONLY, the two height attributes (514 -> 513, 1028 -> 1027) so the markup matches the files actually on main. Released as its own one-line PR by the PROJECT MANAGER and the OWNER AGENT. INTERACTS WITH my own brand-black-optimised (#332, held to morning by the PM): that PR resizes the images to 600x514 / 1200x1028 so the CURRENT attributes become correct, so if #332 is revived it must carry these two numbers back with it. Whichever lands second owns the reconciliation; recorded in both PR bodies. NOT the ?v=2 query, and NOT OwnerInventory/QuoteRequests/Status -- the PM holds those until the contested files clear. | 2026-09-06 |
| mint-session-actor-marker | BUG FIXER (local_8ba9f198) | backend/auth.mjs: owner_sessions gets a nullable `actor TEXT` column (ALTER TABLE, no migrate()/CHECK involved -- rows expire within KMT_SESSION_HOURS, so no rebuild/backfill question the way quotes.decided_by had), createSessionStore().create() and memorySessionStore().create() both take an actor argument so the two stores stay one contract, a new actorFor(id) reader (kept off the hot has() path), mintSession() passes a new MINTED_SESSION_ACTOR ('owner:minted-session', same idiom as quotes.mjs's SHARED_PASSWORD_ACTOR) and the password login path passes its own literal for now. Precondition for #290 (per OWNER AGENT: decided_by must never record a minted session as Ken), not a merge gate on #362 (per PROJECT MANAGER's correction) -- its own PR, landing before #290. Shape signed off by TECHNICAL ARCHITECT (owns the decided_by/actor seam in quotes.mjs); I am not touching quotes.mjs itself, only the primitive #290's resolution code will consume. NOT moving SHARED_PASSWORD_ACTOR out of quotes.mjs -- architect's call, left for whoever lands #290. | 2026-09-07 |
| urgent-reaches-ken | FRONT END LANE / JUNIOR FRONT END DEV 3 (local_16ba9ea6) | src/contact.js (one new export, a bodiless sms href; no change to TEXT_HREF/TEXT_LABEL/TEXT_OPENER), src/components/RequestDetails.jsx (the date-section help and one new action beside it), src/routes/CustomerRequest.jsx (the service-strip line ~437 ONLY -- the urgent action beside the existing /inquiry button). Assigned by the OWNER AGENT on LEAD FRONT END DEVs retirement: an urgent visitor is routed into a 7-day booking or an async form, and the phone path is not offered where urgency is named. Routing only -- no wording invented (labels reuse TEXT_LABEL; the help sentence is Status.jsx:165s ruled line), no change to MIN_LEAD_DAYS, the tyre booking path or /inquiry. THREE-WAY ON ONE FILE, ALL MINE: #332 (held) and #334 (draft) touch CustomerRequest.jsx lines 435-436; this touches ~437. Disjoint lines, and I own all three rebases. | 2026-09-06 |
| owner-google-signin-ui (+ -password-optional) | OWNER AUTH ENGINEER (local_907f8d1f) | TWO BRANCHES, one lane. `owner-google-signin` RELEASED 2026-09-07: #387 merged (355740a), content confirmed on main (`claimsFail` x3, `verifyGoogleClaims` x4). #395 rebased off it onto main and retargeted; the two already-upstream commits were dropped by the rebase, leaving the diff as exactly the two UI files. THIRD ADDED 2026-09-07 on the user's instruction ("PUSH GOOGLE LOGIN RETIRE BUILT IN PASSWORD IT IS INSECURE"), routed by the OWNER AGENT: `owner-password-optional` makes `readAuthConfig` tolerate a missing KMT_OWNER_PASSWORD **when Google is configured**, and makes /api/owner/login refuse outright rather than compare against an empty string. THIS IS THE STEP THAT TURNS UNSETTING THE SECRET FROM A TOTAL OUTAGE INTO A NO-OP: readAuthConfig throws on boot today, so unsetting it stops the server starting and takes the customer wizard, quotes and /status down, not just /owner. It does NOT remove the password and does NOT unset anything. NOT `.github/workflows/` -- step 5 (the gate still passes KMT_OWNER_PASSWORD at fly-deploy.yml:152 and :298, and nothing sets KMT_OWNER_SESSION_COOKIE) is the repo agent's and unowned as far as I know. Earlier scope: PR #387 is the backend half (verifier, callback routes, tests) and PR #395 is the UI half (`src/owner/SignIn.jsx`, the `.oi-signin` CSS), stacked on #387 and unmergeable before it. #395 was held until #375 landed so the new markup carries its data-testid convention from birth; FRONT END ENGINEER (local_0143d9a0) retargets `signInIfAsked`'s two locators after #395 lands -- agreed directly, so neither of us touches the other's file. | Change one of the Google cutover: Google sign-in lands WITH the password still working (#290). `backend/auth.mjs` -- the OIDC callback routes inside `auth.handle` (which is where they must live: `server.mjs:189` runs `auth.handle` before the `/api/` gate at `:191`, so a callback in `api.mjs` would 401 before it could create the session it exists to create) plus a claims-verifying function kept separate from the fetch, so tests can feed it constructed tokens. NOT the session-store region of that file -- `createSessionStore`, `memorySessionStore`, `mintSession` and the actor constants (lines ~33-112, ~290) are BUG FIXER's in #374 (open); I consume `owner_sessions.actor`, I do not modify it. NEW FILE `backend/google-auth.test.mjs` for the verifier tests, deliberately not `backend/owner.test.mjs`, which #374 also holds. `src/owner/SignIn.jsx` -- the Google button beside the existing password field, which stays. `src/owner/OwnerInventory.css` -- the `.oi-signin` block only (lines ~101-107). NOT `quotes.mjs` (TECHNICAL ARCHITECT; `SHARED_PASSWORD_ACTOR` stays put), NOT `owner_sessions`, NOT `.github/` (the CI switch to `KMT_OWNER_SESSION_COOKIE` is the repo agent's), NOT `scripts/mint-session.mjs`, which never gets a route. MEASURED, NOT ASSUMED: #375 (`gate-locator-stability`, open) does NOT touch `SignIn.jsx` -- its files are the five audits, `audit-ui.mjs`, `OwnerInventory.jsx`, `CustomerRequest.jsx`, `QuoteRequests.jsx`. The coupling is one-way: `audit-ui.mjs`'s `signInIfAsked` drives `#owner-password` and `.oi-signin` in my file, so my change breaks their locators whatever order we land in. Rebasing onto #375 per the OWNER AGENT so the new UI carries their testid convention from birth rather than being retrofitted. Their claim row is already released while their PR is open, so this table does not show that file as held.  ADDED 2026-09-07: also `.forge/owner-auth-cutover.md`, ONE APPENDED SECTION AT THE END ONLY (the staleness re-measurement the PROJECT MANAGER and OWNER AGENT asked me to record: the 211- and 120-commit SHA gaps, the sixth `audit-ui.mjs` importer, a11y-85-measure not being in the gate, #362 already merged, `quotes.decided_by` already landed). OVERLAP DECLARED, MEASURED NOT ASSUMED: the TECHNICAL ARCHITECT (local_5b133312) has 122 pending lines in that same file, inserted at line 278 of 352. CORRECTED 2026-09-07: I first recorded these as orphaned on pushed branch `auth-cutover-verifier-tests` with no PR. That branch is dead -- its PR #345 squash-merged while they were still pushing to it -- and the live copy is PR #373 (`rescue-post-merge`), OPEN, confirmed by content not by claim (grep on origin/main = 0, in #373 = 1). Warning for the next sweeper: `git diff --stat origin/main...origin/auth-cutover-verifier-tests` says 122 insertions and will keep saying it after #373 merges, because that branch's merge base predates the squash -- check content on main, not the stat. I am appending at the end, ~74 lines clear of their hunk, so the two should merge without touching each other; whoever lands second owns the reconciliation. Their file, their lane -- flagged to them, not edited around. NOT editing the `#290` reference in `backend/auth.mjs`'s column comment: that is BUG FIXER's open #374. | 2026-09-07 |
| shutdown-drain-check | MAIL DELIVERY ENGINEER (local_8418d5d5) | #285's last piece, placed by the PROJECT MANAGER. NEW FILE `.forge/shutdown-drain-check.mjs` ONLY -- a standalone harness that boots `backend/server.mjs` with a mail adapter configured and a send genuinely in flight, sends a real SIGTERM, and asserts (1) the row reaches `sent` WITH a `provider_id` -- not merely that it left `queued`, since per #399 a `queued` row proves nothing -- and (2) the process exits well inside Fly's 5s `kill_timeout`. **NOT `.github/workflows/fly-deploy.yml`**: ruled by the PROJECT MANAGER (#405 names that file the repo agent's), so JUNIOR REPO AGENT (local_b2ab10bb) wires the one line that calls this, same split as #401. They have advance notice. Why it is needed: `shutdown()` IS invoked on Linux on every check-job teardown (`trap 'kill $server' EXIT` = SIGTERM), but nothing observes the result, AND that job sets no `KMT_MAIL_*` -- so the adapter is NullAdapter, nothing is ever in flight, and it exercises the ordering rather than the drain. If a runner tears down too fast to observe cleanly, that is the finding and it gets reported, not worked around. | 2026-09-07 |
| site-copy-editable | SITE COPY ENGINEER (local_6e40cc27) | The owner-editable-copy build, tier A plus the guards, per the OWNER AGENT rulings recorded in .forge/site-copy-inventory.md (#391). NEW FILES backend/site-copy.mjs (the key registry with the shipped JSX literals as defaults, cleanSiteCopy mirroring cleanReason, and the claim check that warns rather than blocks), backend/site-copy.test.mjs, src/owner/SiteCopy.jsx (a new owner screen -- deliberately NOT a panel inside OwnerInventory.jsx, which brand-black-optimised holds). backend/api.mjs: the new public GET /api/site-copy plus PUBLIC_API_PATHS, and one owner PUT in the dispatch -- NOT the outbox routes, which mail-failure-check, outbox-resolve-ui and outbox-attempt-tracking hold. src/App.jsx: one route line. DEFERRED, NOT CLAIMED YET, because the rows win: src/routes/CustomerRequest.jsx is held on the exact lines I need -- urgent-reaches-ken (#360) rewrites the whole service-strip line 437 carrying all six strip strings, and lockup-height-attributes (#334) holds 435-436 which includes the hero copy. Both are local_16ba9ea6 and both are open; measured by reading #360s diff, not inferred from the row. I build the backend and owner halves first and wire the JSX after those land, coordinating with that session on sequencing and on whether #360s new urgent action is editable copy or a routing control. ALSO OPEN pending the OWNER AGENTs ruling: whether the read path is a mount fetch (adds src/store.js) or a script splice in backend/server.mjs -- I will widen this row rather than assume. WIDENED 2026-09-07: the read path is ruled (injection in backend/static.mjs + backend/server.mjs, both now definite, NOT the shutdown() ordering which outbox-attempt-tracking holds). src/store.js is now definite too but for a different reason than the row first guessed -- the OWNER write path, not the read path: three new exported helpers (ownerSiteCopy/saveSiteCopy/undoSiteCopy) plus ONE line inside call() attaching the parsed body to the thrown error, because a 400 from the copy route carries the conflict list and call() currently keeps only the message. NOT unresolvedOutboxFailures, which owner-mail-alert-badge (local_1fa1cb9a, PR #400) holds in the same file -- disjoint, their region is one new function and does not enter call(). New file src/owner/SiteCopy.jsx and its route line in src/App.jsx. | 2026-09-07 |

| owner-mail-alert-badge | OWNER OPERATIONS ENGINEER (local_1fa1cb9a) | PR #400, open, awaiting a reader (mine -- confirmed to the PROJECT MANAGER, who could not tell from the record; released this row at PR-open instead of at merge, which was the actual gap). src/components/MailAlert.jsx, src/store.js (unresolvedOutboxFailures only), src/owner/OwnerInventory.jsx (.oi-nav region), src/routes/QuoteRequests.jsx (internal-nav region), src/App.css (.mail-alert rule). Reopened to fix a real gap the PROJECT MANAGER's read found: a fetch failure and a genuinely clean outbox both rendered as silence (count stays at its useState(0) default under a blanket .catch), which is exactly the false-negative shape this badge exists to prevent. Fixing now: NeedsSignIn (expected pre-auth, the parent screen's own gate handles it) stays silent; any other error surfaces a distinct "could not check" state instead of reading as clean. Same PR, same branch, no new PR. | 2026-09-07 |
| catalogue-seed-migration-test | OWNER OPERATIONS ENGINEER (local_1fa1cb9a) | PR #413, open, awaiting a reader (holding this row to merge, not releasing at PR-open -- corrected practice per the PROJECT MANAGER's #400 finding). backend/owner.test.mjs -- one new test, region ONLY (the catalogue-lines seeding block, same neighborhood #388 itself just landed there; not the top-of-file imports or the 900+ session-actor tests #374 holds). Per the PROJECT MANAGER's instruction after #388's review: turned the coverage gap I found and proved by hand (two throwaway scripts, deleted, never committed) into a real, rerunnable test -- a genuine close/reopen of a real sqlite file with pre-existing, old-shaped pricing settings (both a real-decided-fee row and a never-touched-default row), opened fresh by seedCatalogueLines() for the first time, asserting amounts and isPlaceholder correctly both directions. Proven able to fail (mutated the seed to always report isPlaceholder: true, watched the new test go red naming exactly that, reverted). Not a blocker on anything; #388 already merged and deployed (2841888). No production code change -- test only, pins behavior already verified live and already shipped. | 2026-09-07 |
`scraper-catalog-updater`, `codex/refine-order-flow`, `wire-scraped-catalog` and
`owner-inventory-backend` were all merged into `main` on 2026-09-05 and their
rows removed. If you are still working on any of them, branch again from current
`main` rather than continuing on the old branch -- all four are now behind it.
